# Browser calls same-origin /backend/* (Next rewrite → API).
NEXT_PUBLIC_API_BASE_URL=/backend

# Server-side rewrite target (Nest API).
API_INTERNAL_URL=http://localhost:3001

NEXT_PUBLIC_APP_URL=http://localhost:3000
