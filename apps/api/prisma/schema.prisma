// AutoPilot API — Prisma schema
// Entities will be added in a dedicated modeling stage.
// See docs/database-principles.md for multi-tenancy, soft delete, audit and events.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
